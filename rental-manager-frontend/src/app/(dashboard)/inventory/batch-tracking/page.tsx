"use client";

import { BatchTrackingDashboard } from '@/components/inventory';
import { PageWrapper } from '@/components/layout/page-wrapper';

export default function BatchTrackingPage() {
  return (
    <PageWrapper>
      <BatchTrackingDashboard />
    </PageWrapper>
  );
}